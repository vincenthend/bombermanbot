bool isSafe(const GameState& G, const Point& CurPos){
	bool safe;
	int i;
	safe = true;
	
	//find bomb Vertically
	if(G.GB[ChrPos.x][ChrPos.y+1].Entity 
		for(i=1; i<= G.MapHeight; i++){
			if(G.GB[ChrPos.x][i].Entity == "Bomb"){
				//find bomb ID
			}
		}
	
	
	return safe;
}